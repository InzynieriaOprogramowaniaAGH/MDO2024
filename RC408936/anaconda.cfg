# Generated by Anaconda 38.23.4
# Generated by pykickstart v3.47
#version=F38
# Use graphical install
graphical

# Keyboard layouts
keyboard --vckeymap=pl --xlayouts='pl'
# System language
lang pl_PL.UTF-8

# Use CDROM installation media
cdrom

%packages
@^server-product-environment

%end

# Run the Setup Agent on first boot
firstboot --enable

# Generated using Blivet version 3.7.1
ignoredisk --only-use=sda
autopart

# System timezone
timezone Europe/Warsaw --utc

#Root password
rootpw --lock
user --groups=wheel --name=kszanu --password=$y$j9T$WmSRC3Yq>


# System bootloader configuration
bootloader --location=mbr --boot-drive=sda

# Clear the Master Boot Record
zerombr

# Partition clearing information
clearpart --all --initlabel

# Disk partitioning information
autopart

%packages
@^server-product-environment
@headless-management
# Dodaj tutaj dodatkowe pakiety z maszyny Komputer2
nano
tree
ansible
docker

%end

%post --log=/root/ks-post.log
#!/bin/bash
# Uruchomienie i konfiguracja Dockera
systemctl start docker
systemctl enable docker

# Uruchomienie kontenera lighttpd
docker run -d --name my-lighttpd-container -p 80:80 docker.io/library/lighttpd

# Zapewnienie, że Docker uruchomi się przy starcie systemu
systemctl enable docker
%end

# Additional repositories
url --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=fedora-38&arch=x86_64
repo --name=updates --mirrorlist=http://mirrors.fedoraproject.org/mirrorlist?repo=updates-released-f38&arch=x86_64

# Run the Setup Agent on first boot
firstboot --enable